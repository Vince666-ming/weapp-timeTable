<van-button size="large" plain type="info" bindtap="delete">清空</van-button>
<form bindsubmit="submit" bindreset="reset">
 <picker mode="date" name="date" value="{{date}}" bindchange="bindDateChange">
  <view class="picker">
    <text>日期：</text> {{date}}（点击选择）
  </view>
</picker>
<view class='xian'></view>
<van-cell-group>
  <van-field value="{{clear}}"  name="six" label="6:00-7:00" type="textarea" placeholder="输入计划事项（9字以内）" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="sixDetails" label="细节：" type="textarea" placeholder="输入细节信息（可不填）" autosize border="{{ true }}" />
  <van-field value="{{clear}}"  name="seven" label="7:00-8:00" type="textarea" placeholder="输入计划事项（9字以内）" maxlength="9"   border="{{ true }}" />
  <van-field value="{{clear}}"  name="sevenDetails" label="细节：" type="textarea" placeholder="输入细节信息（可不填）"  autosize border="{{ true }}" />
  <!-- 8 -->
  <van-field value="{{clear}}"  name="eight" label="8:00-9:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="eightDetails" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 9 -->
  <van-field value="{{clear}}"  name="nine" label="9:00-10:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="nineDetails" label="细节：" type="textarea" placeholder="输入细节信息"   autosize border="{{ true }}" />
  <!-- 10 -->
  <van-field value="{{clear}}"  name="ten" label="10:00-11:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="tenDetails" label="细节：" type="textarea" placeholder="输入细节信息"   autosize border="{{ true }}" />
  <!-- 11 -->
  <van-field value="{{clear}}"  name="eleven" label="11:00-12:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="elevenDetails" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 12 -->
  <van-field value="{{clear}}"  name="twelve" label="12:00-13:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="twelveDetails" label="细节：" type="textarea" placeholder="输入细节信息" autosize border="{{ true }}" />
  <!-- 13 -->
  <van-field value="{{clear}}"  name="thirteen" label="13:00-14:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="thirteenDetails" label="细节：" type="textarea" placeholder="输入细节信息" autosize border="{{ true }}" />
  <!-- 14 -->
  <van-field value="{{clear}}"  name="fourteen" label="14:00-15:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="fourteenDetails" label="细节：" type="textarea" placeholder="输入细节信息" utosize border="{{ true }}" />
  <!-- 15 -->
  <van-field value="{{clear}}"  name="fifteen" label="15:00-16:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="fifteenDetails" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 16 -->
  <van-field value="{{clear}}"  name="sixteen" label="16:00-17:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="sixteenDetails" label="细节：" type="textarea" placeholder="输入细节信息" autosize border="{{ true }}" />
  <!-- 17 -->
  <van-field value="{{clear}}"  name="seventeen" label="17:00-18:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="seventeenDetails" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 18 -->
  <van-field value="{{clear}}"  name="eighteen" label="18:00-19:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="eighteenDetails" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 19 -->
  <van-field value="{{clear}}"  name="nineteen" label="19:00-20:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="nineteenDetails" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 20 -->
  <van-field value="{{clear}}"  name="twenty" label="20:00-21:00" type="textarea" placeholder="输入计划事项" maxlength="9"  border="{{ true }}" />
  <van-field value="{{clear}}"  name="twentyDetails" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 21 -->
  <van-field value="{{clear}}"  name="twenty1" label="21:00-22:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="twenty1Details" label="细节：" type="textarea" placeholder="输入细节信息"  autosize border="{{ true }}" />
  <!-- 22 -->
  <van-field value="{{clear}}"  name="twenty2" label="22:00-23:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="twenty2Details" label="细节：" type="textarea" placeholder="输入细节信息" autosize border="{{ true }}" />
  <!-- 23 -->
  <van-field value="{{clear}}"  name="twenty3" label="23:00-24:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="twenty3Details" label="细节：" type="textarea" placeholder="输入细节信息" autosize border="{{ true }}" />
  <!-- 24 -->
  <van-field value="{{clear}}"  name="twenty4" label="00:00-01:00" type="textarea" placeholder="输入计划事项" maxlength="9" border="{{ true }}" />
  <van-field value="{{clear}}"  name="twenty4Details" label="细节：" type="textarea" placeholder="输入细节信息" autosize border="{{ true }}" />
  <van-field value="{{clear}}"  name="remarks" label="备注：" type="textarea" placeholder="输入备注信息"  autosize border="{{ true }}" />
</van-cell-group>
<!-- <view form-type="submit">提交时间表</view> -->
  <button  plain type="default" form-type="submit">提交时间表</button>
</form>
<navigator class="item" url="/pages/show/show">
  <van-button size="large" plain type="primary">查看我的时间表</van-button>
</navigator> 